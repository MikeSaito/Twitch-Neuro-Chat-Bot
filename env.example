# ============================================
# Twitch Neuro Chat Bot - Пример конфигурации
# ============================================
# Скопируйте этот файл в .env и заполните своими значениями
# cp env.example .env
# или
# copy env.example .env

# ============================================
# Twitch настройки (ОБЯЗАТЕЛЬНО)
# ============================================
# Имя бота в Twitch
TWITCH_USERNAME=your_bot_username

# OAuth токен бота (получить на https://twitchtokengenerator.com/)
# Формат: oauth:xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWITCH_OAUTH_TOKEN=oauth:your_token_here

# Канал для подключения (без #)
TWITCH_CHANNEL=channel_name

# Имя стримера (для идентификации голоса)
STREAMER_NAME=streamer_name

# ============================================
# ProxyAPI настройки (ОБЯЗАТЕЛЬНО для Gemini)
# ============================================
# Использовать ProxyAPI вместо прямого OpenAI
USE_PROXYAPI=true

# API ключ ProxyAPI
PROXYAPI_KEY=your_proxyapi_key_here

# URL ProxyAPI (обычно не нужно менять)
PROXYAPI_URL=https://api.proxyapi.ru

# Провайдер: google (для Gemini), openai, anthropic
PROXYAPI_PROVIDER=google

# Модель для анализа изображений и генерации сообщений
# Доступные: gemini-2.5-flash-lite, gemini-2.0-flash-exp, gpt-4o, gpt-4o-2024-11-20, gpt-4o-2024-08-06
PROXYAPI_VISION_MODEL=gemini-2.5-flash-lite

# Модель для распознавания речи (Whisper через ProxyAPI)
# Доступные: gpt-4o-transcribe, gpt-4o-mini-transcribe
PROXYAPI_WHISPER_MODEL=gpt-4o-transcribe

# ============================================
# OpenAI настройки (опционально, если не используете ProxyAPI)
# ============================================
# API ключ OpenAI (если не используете ProxyAPI)
OPENAI_API_KEY=your_openai_api_key_here

# ============================================
# Локальный Whisper (опционально)
# ============================================
# Использовать локальный Whisper вместо ProxyAPI
# Требует установки: pip install faster-whisper
USE_LOCAL_WHISPER=false

# Модель локального Whisper
# Доступные: tiny, base, small, medium, large, large-v2, large-v3
# Рекомендуется: small (баланс скорости и качества)
LOCAL_WHISPER_MODEL=small

# Устройство для обработки: cpu или cuda
LOCAL_WHISPER_DEVICE=cpu

# Тип вычислений: int8 (быстро), int8_float16 (CUDA), float16, float32 (медленнее, но лучше)
LOCAL_WHISPER_COMPUTE_TYPE=int8

# Размер луча для поиска: 1 (быстро), 2-3 (баланс), 5 (медленнее, но лучше)
LOCAL_WHISPER_BEAM_SIZE=2

# ============================================
# Браузер настройки
# ============================================
# Запускать браузер в headless режиме (без окна)
# true = без окна, false = с окном
HEADLESS=true

# Интервал создания скриншотов (в миллисекундах)
# 10000 = 10 секунд
SCREENSHOT_INTERVAL=10000

# Максимальный возраст скриншотов в минутах (для очистки)
SCREENSHOT_MAX_AGE=60

# Путь к FFmpeg (если не в PATH)
# FFMPEG_PATH=C:\path\to\ffmpeg.exe

# ============================================
# Координатор настройки
# ============================================
# Минимальная уверенность для распознавания речи (0.0 - 1.0)
MIN_CONFIDENCE=0.7

# Порог тишины для VAD (0.0 - 1.0)
SILENCE_THRESHOLD=0.3

# Режим работы мозга: normal (основной) или training (обучение)
BRAIN_MODE=normal

# ============================================
# Отладка
# ============================================
# Только консольный вывод, не отправлять сообщения в чат
# true = только консоль, false = отправлять в чат
CONSOLE_ONLY=false

# ============================================
# Сбор данных (опционально)
# ============================================
# Включить сбор данных для обучения
# false = отключен, true или не указано = включен
ENABLE_DATA_COLLECTION=true
