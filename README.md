# Twitch Neuro Chat Bot

Умный бот для Twitch чата с нейросетевым анализом стрима. Бот обучается на чате, анализирует стрим в реальном времени и генерирует естественные сообщения, неотличимые от обычных зрителей.

## 🧠 Архитектура

### Основные компоненты

1. **Мозг (BrainCoordinator)** - центральный координатор
   - Управляет всеми модулями
   - Оптимизирует промпты для ИИ
   - Принимает решения о генерации сообщений
   - Имеет память для хранения важной информации
   - Режимы работы: основной и обучение

2. **Виртуальный браузер** (`src/modules/browser.js`)
   - Использует Playwright для автоматизации браузера
   - Делает скриншоты стрима каждые 5 секунд
   - Захватывает аудио из стрима через yt-dlp и ffmpeg

3. **Анализ изображений** (`src/modules/imageAnalyzer.js`)
   - Использует локальный LLM (llava/llava-phi3/llava-1.6) или GPT-4 Vision через ProxyAPI
   - Описывает действия, текст, интерфейс и атмосферу стрима
   - Мозг может дописывать подробности к промпту
   - **Рекомендация:** Для лучшей точности используйте ProxyAPI (GPT-4o) или llava-phi3/llava-1.6
   - **Важно:** Базовая модель `llava` может быть неточной. Используйте `llava-phi3` или `llava-1.6` для лучших результатов

4. **Распознавание речи** (`src/modules/speechRecognizer.js`)
   - Использует локальный Whisper (faster-whisper) или OpenAI Whisper
   - Преобразует аудио в текст
   - Умный захват голоса (VAD) - только когда человек говорит

5. **Идентификация голосов** (`src/modules/localVoiceIdentifier.js`)
   - Определяет кто говорит (стример, гость, донат)
   - Фильтрует донаты и TTS сообщения
   - Запоминает новых людей

6. **Генерация сообщений** (`src/modules/messageGenerator.js`)
   - Использует локальный LLM (llama2/llama3/qwen2.5) или GPT-4 через ProxyAPI
   - Генерирует естественные сообщения
   - Ограничен ТОЛЬКО банвордами Twitch
   - Мозг может полностью редактировать промпт (кроме банвордов)
   - **Рекомендация:** Для лучшего качества используйте `llama3.1` или `qwen2.5` (отлично понимает русский)

7. **Чтение чата** (`src/modules/chatReader.js`)
   - Анализирует сообщения в чате
   - Находит интересные сообщения для ответа
   - Мозг может полностью редактировать промпт

8. **Правая рука** (`src/modules/rightHand.js`)
   - Выполняет сложные задачи по поручению мозга
   - Мозг может полностью управлять

9. **Помощник мозга** (`src/modules/brainAssistant.js`)
   - Помогает мозгу с сложными задачами
   - Делегируется мозгом при необходимости

10. **Модуль обучения** (`src/modules/brainTrainer.js`)
    - Обучается на сообщениях из чата
    - Создает промпты для ChatReader и MessageGenerator
    - Работает в режиме обучения

## 🎯 Возможности Мозга

### Полная свобода
- Полная свобода в принятии решений
- Может редактировать промпты (кроме защищенных секций)
- Может экспериментировать и пробовать новые подходы
- Использует память и контекст для улучшения решений

### Ограничения Мозга
- НЕ может менять секции про банворды Twitch в промптах генерации сообщений
- НЕ может менять основной промпт анализа изображений (только дописывать подробности)
- ДОЛЖЕН избегать запрещенных тем (экстремизм, насилие, дискриминация)
- ДОЛЖЕН избегать политических тем (политика, выборы, партии)

### Ограничения Генератора Сообщений
- ТОЛЬКО банворды Twitch - нельзя использовать слова, которые могут привести к бану
- ВСЕ ОСТАЛЬНОЕ разрешено: мат, пошлости, политика, хамство, любые темы

## 📋 Требования

- Node.js 18+
- npm или yarn
- Twitch OAuth токен для бота
- (Опционально) OpenAI API ключ (если не используете локальные модели)
- (Опционально) Ollama для локальных LLM
- (Опционально) Python с faster-whisper для локального распознавания речи
- yt-dlp и ffmpeg для захвата аудио

## 🚀 Установка

### 1. Установка зависимостей

```bash
# Установите зависимости
npm install

# Установите браузеры для Playwright
npx playwright install chromium
```

### 2. Установка локальных моделей (рекомендуется)

#### Ollama (для LLM и Vision)

```bash
# Windows: скачайте с https://ollama.ai
# Linux/Mac:
curl -fsSL https://ollama.ai/install.sh | sh

# Установите модели для генерации сообщений (выберите одну):
ollama pull llama2             # Базовая модель (быстрая, но менее точная)
ollama pull llama3             # Улучшенная версия (рекомендуется) ✅
ollama pull llama3.1           # Самая новая версия (лучше всего) ✅✅
ollama pull mistral            # Альтернативная модель (хорошая для чата)
ollama pull qwen2.5            # Китайская модель (отлично понимает русский) ✅
ollama pull phi3               # Легкая но умная модель

```

#### faster-whisper (для распознавания речи)

```bash
pip install faster-whisper
```

#### yt-dlp и ffmpeg (для захвата аудио)

```bash
# Windows (через pip)
pip install yt-dlp

# ffmpeg: скачайте с https://www.gyan.dev/ffmpeg/builds/
# Или через chocolatey: choco install ffmpeg
# Или через winget: winget install ffmpeg

# Linux/Mac
pip3 install yt-dlp
# ffmpeg обычно уже установлен
```

### 3. Настройка

Создайте файл `.env`:

```env
# Twitch настройки
TWITCH_USERNAME=your_bot_username
TWITCH_OAUTH_TOKEN=oauth:your_token
TWITCH_CHANNEL=channel_name
STREAMER_NAME=streamer_name

# OpenAI API (опционально, если не используете локальные модели)
OPENAI_API_KEY=sk-...

# Локальные модели (рекомендуется)
USE_LOCAL_WHISPER=true
USE_LOCAL_LLM=true
USE_LOCAL_VISION=true
# Для генерации сообщений выберите одну из моделей:
# llama2 - базовая (быстрая, но менее точная) ❌
# llama3 - улучшенная (рекомендуется) ✅
# llama3.1 - самая новая (лучше всего) ✅✅
# mistral - альтернативная модель (хорошая для чата)
# qwen2.5 - отлично понимает русский язык ✅✅
# phi3 - легкая но умная модель
OLLAMA_MODEL=llama3.1
# Для анализа изображений выберите одну из моделей:
# llava - базовая (быстрая, но менее точная) ❌ НЕ РЕКОМЕНДУЕТСЯ
# llava-phi3 - более точная (рекомендуется) ✅
# llava-1.6 - самая точная (медленнее, но лучше) ✅✅
# bakllava - альтернативная модель
# moondream - легкая но точная
OLLAMA_VISION_MODEL=llava-phi3
LOCAL_WHISPER_MODEL=small  # base (быстро, но хуже), small (рекомендуется ✅), medium (лучше, но медленнее), large (лучше всего, но очень медленно)
OLLAMA_URL=http://localhost:11434

# Настройки браузера
HEADLESS=true
SCREENSHOT_INTERVAL=5000

# Настройки координатора
MIN_CONFIDENCE=0.7
SILENCE_THRESHOLD=0.3

# Режим работы мозга (normal или training)
BRAIN_MODE=normal

# Режим отладки
CONSOLE_ONLY=false
```

### 4. Получение Twitch OAuth токена

1. Перейдите на https://twitchtokengenerator.com/
2. Нажмите "Connect with Twitch" и авторизуйтесь
3. Скопируйте OAuth токен (формат: `oauth:xxxxx`)
4. Вставьте в `.env` как `TWITCH_OAUTH_TOKEN`

## 🎮 Запуск

```bash
# Обычный запуск
npm start

# Запуск с автоперезагрузкой (для разработки)
npm run dev
```

## 🎛️ Команды управления

В чате Twitch можно использовать команды:

- `!bot silence` - Включить режим молчания
- `!bot unsilence` - Выключить режим молчания
- `!bot stats` - Показать статистику
- `!bot mode` - Переключить режим работы мозга (основной/обучение)
- `!bot training` - Включить режим обучения
- `!bot normal` - Включить основной режим
- `!bot memory` - Показать статистику памяти
- `!bot forget` - Очистить память

## 📊 Как это работает

1. Бот подключается к Twitch чату указанного канала
2. Виртуальный браузер открывает стрим и делает скриншоты каждые 5 секунд
3. Скриншот анализируется локальным LLM (llava/llava-phi3/llava-1.6) или GPT-4 Vision через ProxyAPI
4. Аудио стрима захватывается через yt-dlp и ffmpeg
5. Речь распознается через локальный Whisper (faster-whisper) или OpenAI Whisper
6. **Мозг анализирует контекст:**
   - Определяет, кто говорит (стример, гость, донат)
   - Фильтрует донаты и TTS сообщения
   - Оптимизирует промпты для ИИ
   - Принимает решение о генерации сообщения
7. Генератор сообщений создает естественное сообщение
8. Сообщение проверяется на банворды Twitch
9. Сообщение отправляется в чат

## 🧠 Режимы работы

### Основной режим (normal)
- Мозг работает как чатерс
- Генерирует сообщения в чат
- Оптимизирует промпты на основе контекста
- Сохраняет созданные промпты в файлы

### Режим обучения (training)
- Мозг получает сообщения из чата напрямую
- Анализирует паттерны сообщений
- Создает промпты для ChatReader и MessageGenerator
- Сохраняет созданные промпты в файлы
- НЕ отправляет сообщения в чат

## 💾 Память мозга

Мозг имеет структурированную память для хранения важной информации:

- Записывает информацию о оптимизации промптов
- Хранит паттерны из чата
- Запоминает контекст стрима
- Автоматически управляет размером памяти (максимум 1000 записей)

Память хранится в `resources/brain_memory.json`

## 📁 Структура проекта

```
Нейро бот/
├── src/
│   ├── modules/
│   │   ├── brainCoordinator.js    # Мозг - центральный координатор
│   │   ├── brainMemory.js         # Память мозга
│   │   ├── brainTrainer.js        # Модуль обучения
│   │   ├── brainAssistant.js      # Помощник мозга
│   │   ├── rightHand.js           # Правая рука мозга
│   │   ├── chatReader.js          # Чтение чата
│   │   ├── messageGenerator.js    # Генерация сообщений
│   │   ├── imageAnalyzer.js       # Анализ изображений
│   │   ├── speechRecognizer.js    # Распознавание речи
│   │   ├── localWhisper.js        # Локальный Whisper
│   │   ├── localLLM.js            # Локальный LLM
│   │   ├── localVoiceIdentifier.js # Идентификация голосов
│   │   ├── browser.js             # Виртуальный браузер
│   │   ├── coordinator.js         # Главный координатор
│   │   ├── promptManager.js       # Менеджер промптов
│   │   └── ...
│   ├── config.js                  # Конфигурация
│   ├── index.js                   # Точка входа
│   └── twitchClient.js            # Twitch клиент
├── resources/
│   ├── prompts/                   # Промпты, созданные мозгом
│   │   ├── image/
│   │   ├── message/
│   │   ├── chatreader/
│   │   └── righthand/
│   └── brain_memory.json          # Память мозга
├── data/
│   └── voices.json                # База данных голосов
├── scripts/
│   └── whisper_local.py          # Скрипт для faster-whisper
├── package.json
└── README.md
```

## ⚠️ Важные замечания

- **API расходы**: При использовании OpenAI API могут быть затраты
- **Локальные модели**: Рекомендуется использовать локальные модели для экономии
- **Производительность**: Анализ изображений и речи занимает время
- **Twitch ToS**: Убедитесь, что использование бота соответствует правилам Twitch
- **Режим молчания**: Координатор может автоматически включать режим молчания при низком качестве данных

## 🐛 Решение проблем

### Бот не подключается к Twitch
- Проверьте правильность OAuth токена
- Убедитесь, что имя бота и канал указаны верно

### Ошибки локальных моделей
- Проверьте, что Ollama запущен: `ollama serve`
- Убедитесь, что модели установлены: `ollama list`
- Проверьте, что faster-whisper установлен: `python -c "import faster_whisper"`

### Локальная модель для анализа изображений не узнает ничего
- **Базовая модель `llava` может быть неточной.** Установите более мощную модель:
  ```bash
  # Рекомендуется: llava-phi3 (баланс скорости и точности)
  ollama pull llava-phi3
  
  # Или самая точная: llava-1.6 (медленнее, но лучше)
  ollama pull llava-1.6
  
  # Затем в .env файле укажите:
  OLLAMA_VISION_MODEL=llava-phi3  # или llava-1.6
  ```
- **Или используйте ProxyAPI для vision** (GPT-4o намного мощнее):
  ```env
  USE_LOCAL_VISION=false
  USE_PROXYAPI=true
  PROXYAPI_VISION_MODEL=gpt-4o-2024-11-20
  ```

### Браузер не запускается
- Установите браузеры: `npx playwright install chromium`
- Проверьте права доступа

### Аудио не захватывается
- Убедитесь, что yt-dlp установлен: `yt-dlp --version`
- Убедитесь, что ffmpeg установлен: `ffmpeg -version`
- Проверьте, что стрим идет в прямом эфире

## 📝 Лицензия

MIT
