# Twitch Neuro Chat Bot

Умный бот для Twitch чата с нейросетевым анализом стрима в реальном времени. Бот анализирует видео через Google Gemini, распознает речь стримера с помощью Whisper и генерирует естественные сообщения в чат.

## 🚀 Основные возможности

- **Анализ стрима в реальном времени**: Автоматический анализ скриншотов стрима через Google Gemini 2.5 Flash Lite
- **Распознавание речи**: Определение речи стримера и гостей с помощью Whisper (локально или через ProxyAPI)
- **Генерация сообщений**: Умная генерация сообщений на основе контекста стрима через Gemini (видит стрим напрямую!)
- **Идентификация голосов**: Различение голоса стримера, гостей и донатов
- **История сессий**: Сохранение контекста между перезапусками
- **Гибкая конфигурация**: Поддержка локального Whisper и внешних API (ProxyAPI)

## 📋 Требования

- Node.js 18+
- Python 3.8+ (для локального Whisper, опционально)
- FFmpeg (для захвата аудио)
- yt-dlp (для получения URL стрима)

## 🔧 Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd twitch-neuro-chat-bot
```

2. Установите зависимости:
```bash
npm install
```

3. Установите Python зависимости для локального Whisper (опционально):
```bash
pip install faster-whisper
```

4. Создайте файл `.env` на основе примера `env.example`:
```bash
cp env.example .env
# или на Windows:
copy env.example .env
```

Затем отредактируйте `.env` и заполните своими значениями. Пример содержимого:
```env
# Twitch
TWITCH_USERNAME=your_bot_username
TWITCH_OAUTH_TOKEN=oauth:your_token
TWITCH_CHANNEL=channel_name
STREAMER_NAME=streamer_name

# ProxyAPI (обязательно для Gemini)
USE_PROXYAPI=true
PROXYAPI_KEY=your_proxyapi_key
PROXYAPI_URL=https://api.proxyapi.ru
PROXYAPI_PROVIDER=google
PROXYAPI_VISION_MODEL=gemini-2.5-flash-lite
PROXYAPI_WHISPER_MODEL=gpt-4o-transcribe

# Локальный Whisper (опционально)
USE_LOCAL_WHISPER=false
LOCAL_WHISPER_MODEL=small
LOCAL_WHISPER_DEVICE=cpu
LOCAL_WHISPER_COMPUTE_TYPE=int8

# Браузер
HEADLESS=true
SCREENSHOT_INTERVAL=10000

# Режим работы
BRAIN_MODE=normal
```

## 🎮 Запуск

### Основной режим
```bash
npm start
```

## 🧠 Режимы работы

### Основной режим (normal)
Бот анализирует стрим и генерирует сообщения в чат на основе:
- Анализа скриншотов стрима через Gemini (видит стрим напрямую!)
- Распознанной речи стримера
- Истории сессий

**Особенность**: Gemini видит стрим напрямую и сразу генерирует сообщения, что делает бота более активным и контекстным.

### Режим обучения (training)
Бот собирает данные из чата для анализа:
- Сохраняет сообщения из чата
- Сохраняет контекст (речь)
- Подготавливает данные для обучения

Для включения режима обучения:
```env
BRAIN_MODE=training
```

## 📝 Команды бота

Команды можно отправлять в Twitch чат или в терминал:

- `!bot silence` - включить режим молчания
- `!bot unsilence` - выключить режим молчания
- `!bot stats` - показать статистику
- `!bot mode` - показать текущий режим работы мозга
- `!bot training` - включить режим обучения
- `!bot normal` - включить основной режим
- `!bot memory` - показать статистику памяти
- `!bot forget` - очистить память
- `!bot screenshot` - запросить скриншот от мозга

## 🏗️ Архитектура

### Основные модули

- **BrainCoordinator** - координатор работы мозга, управление памятью и генерация сообщений
- **ImageAnalyzer** - анализ скриншотов стрима через Gemini и генерация сообщений
- **SpeechRecognizer** - распознавание речи (Whisper локально или через ProxyAPI)
- **VoiceIdentifier** - идентификация говорящего (стример/гость/донат)
- **Coordinator** - координация работы модулей
- **SessionHistory** - сохранение истории сессий
- **BrainTrainer** - модуль обучения (режим training)
- **BrainMemory** - долгосрочная память бота

### Потоки данных

1. **Скриншоты** → ImageAnalyzer (Gemini) → Coordinator
2. **Аудио** → SpeechRecognizer (Whisper) → VoiceIdentifier → Coordinator
3. **Чат** → Coordinator → BrainTrainer (в режиме обучения)
4. **Coordinator** → Gemini (через ImageAnalyzer) → TwitchClient → Чат

### Особенности реализации

- **Прямая генерация через Gemini**: Gemini видит скриншот стрима и сразу генерирует сообщение, что делает бота более активным
- **Оптимизированная обработка речи**: Используется буферизация и периодический анализ для снижения нагрузки
- **Автоматический fallback**: При ошибках памяти или таймаутах Whisper автоматически переключается на меньшую модель
- **Фильтрация артефактов**: Автоматическая фильтрация артефактов распознавания речи

## 📁 Структура проекта

```
twitch-neuro-chat-bot/
├── src/
│   ├── index.js              # Точка входа
│   ├── config.js             # Конфигурация
│   ├── twitchClient.js       # Клиент Twitch
│   └── modules/              # Модули бота
│       ├── brainCoordinator.js  # Координатор мозга
│       ├── imageAnalyzer.js      # Анализ изображений и генерация сообщений
│       ├── speechRecognizer.js   # Распознавание речи
│       ├── voiceIdentifier.js    # Идентификация голосов
│       ├── coordinator.js        # Координация модулей
│       ├── brainMemory.js        # Долгосрочная память
│       ├── sessionHistory.js     # История сессий
│       ├── brainTrainer.js       # Модуль обучения
│       ├── localWhisper.js       # Локальный Whisper
│       ├── proxyAPI.js           # ProxyAPI клиент
│       └── ...
├── scripts/
│   └── whisper_local.py      # Локальный Whisper (опционально)
├── data/                     # Данные (не в git)
│   ├── streamer_info.json
│   └── session_history.json
├── resources/                # Ресурсы (не в git)
│   └── brain_memory.json
├── .env                      # Конфигурация (не в git)
└── env.example               # Пример конфигурации
```

## ⚙️ Конфигурация

### Информация о стримере

Создайте файл `data/streamer_info.json` с информацией о стримере:

```json
{
  "name": "streamer_name",
  "displayName": "Display Name",
  "personality": {
    "description": "Описание характера",
    "communicationStyle": "Стиль общения",
    "interests": ["Интерес 1", "Интерес 2"]
  },
  "chatStyle": {
    "preferredLength": "Короткие сообщения",
    "punctuation": "Минимум знаков препинания",
    "emotions": "Умеренное использование смайликов"
  }
}
```

### Модели Gemini

По умолчанию используется `gemini-2.5-flash-lite` для анализа изображений и генерации сообщений. Модель видит стрим напрямую и генерирует сообщения на основе визуального контекста.

## 🔒 Ограничения

Бот автоматически фильтрует:
- Банворды Twitch
- Политические темы
- Сообщения на не-русском языке (кроме английского в небольших количествах)
- Обычные эмодзи (разрешены только 7TV эмодзи)
- Знаки препинания (кроме "?" и изредка ",")

Все остальное разрешено (мат, пошлости, хамство и т.д.)

## ⚡ Оптимизации

- **Оптимизированная обработка массивов**: Использование циклов вместо цепочек filter/map для лучшей производительности
- **Кэширование артефактов**: Set для быстрой проверки артефактов распознавания речи
- **Буферизация речи**: Накопление фраз перед анализом для снижения нагрузки
- **Автоматический fallback**: Переключение на меньшие модели при ошибках памяти

## 🐛 Решение проблем

### Ошибка подключения к ProxyAPI
Проверьте `PROXYAPI_KEY` в `.env` файле.

### Ошибка распознавания речи
Если используете локальный Whisper, убедитесь, что установлен `faster-whisper`:
```bash
pip install faster-whisper
```

Если локальный Whisper недоступен, бот автоматически переключится на ProxyAPI.

### Недостаточно памяти для Whisper
Бот автоматически переключается на меньшую модель при ошибках памяти или таймаутах. Используется иерархия моделей: large-v3 → large-v2 → large → medium → small → base → tiny.

### Бот часто возвращает "null"
Убедитесь, что:
- Стрим запущен и доступен
- Скриншоты создаются успешно
- ProxyAPI работает корректно
- Gemini получает корректные данные

### Бот пишет на английском
В системном промпте прописано обязательное использование только русского языка. Если проблема сохраняется, проверьте промпт в `brainCoordinator.js`.